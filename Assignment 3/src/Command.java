/**
 * The Command class, the interface for the main program to perform actions on the vending machine
 *
 * @author Po Yat Ching David UID:3035372098
 */
public class Command {
    /**
     * Execute string.
     *
     * @param v       the Vending Machine object
     * @param cmdPart the Parameters of the command
     * @return the result of the command
     */
    public String execute(VendingMachine v, String cmdPart){
        String result = null;
        return result;
    }
}
